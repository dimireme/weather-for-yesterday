export const SimpleLayout = ({ children }: { children: React.ReactNode }) => (
  <main className="min-h-screen flex items-center justify-center bg-gray-100 max-w-2xl p-8">
    {children}
  </main>
);
